# Git 基础命令汇总
## 一、仓库基础查看命令
1. git status   # 查看本地文件修改、暂存状态，如果为红色就是修改但为到暂存区，如果是绿色代表已经提交到暂存区了
2. git log # 查看全部历史提交记录，q退出日志界面
3. git log --oneline # 精简版日志
4.git pull # 拉取云端远程在最新代码，同步本地仓库

## 二、文件提交上传完整流程
1. git add . # 将本地所有改动加入暂存区
2. git commit -m "备注信息" # 本地永久保存此次修改快照
3. git push # 将本地提交上传至云端远程仓库

## 三、撤销/删除文件操作
1. git reset . # 撤销全部文件的暂存，文件保留在本地
2. git rm 文件名 # 本地物理删除文件 +将删除操作加入暂存区

## 四、分支操作基础命令
1. git branch    # 查看本地所有分支，星号在哪里目前就在哪个分支
2. git branch 分支名  # 创建指定名称的分支
3. git switch 分支名  # 切换到指定的分支
4. git push -u origin 分支名 # 首次推送新建分支到远程仓库
5. git branch -d 分支名 # 删除本地分支（需要切换到main分支）
6. git push origin --delete 分支名 #删除远程分支
